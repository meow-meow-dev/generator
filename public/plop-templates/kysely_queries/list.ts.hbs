import type {
  {{ name }},
  DB,
} from "#server/types/database";
import type { Selectable } from "kysely";

import { listQuery } from "@meow-meow-dev/server-utilities/queries";

export const list{{ pluralName }} = listQuery<
  DB,
  Record<string, unknown>,
  Selectable<{{ name }}>
>(({ db }) =>
  db
    .selectFrom("{{ tableName }}")
    .select([
      /* TODO list fields */
    ])
    .execute(),
);
