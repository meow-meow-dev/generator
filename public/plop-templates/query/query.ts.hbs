import type { UseSuspenseQueryOptions } from "@tanstack/react-query";

import { {{ name }} } from "{{ projectName }}/client/rpc";

export function {{ name }}Query(): UseSuspenseQueryOptions<Awaited<ReturnType<typeof {{ name }}>>> {
  return {
    queryFn: {{ name }},
    queryKey: [/* XXX */],
  } as const;
}
